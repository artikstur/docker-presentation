# Имя локального образа
FROM node:22-alpine

# Указание рабочей директори. При запуске образа докер пытается ее найти, если ее нет, создаст сам
WORKDIR /app

# Путь относительно DockerFile, путь внутри образа
COPY . .

# Выполняем команду при создании образа. 
# Нужно только для установки зависимостей в образ
RUN npm install

EXPOSE 3000

# Команды, которые должны выполнится при запуске контейнера
CMD ["npm", "run", "dev"]
